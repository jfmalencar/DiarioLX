# Invalid field

> HTTP `400` · `application/problem+json`

**Code:** `invalid-field`

The provided field is invalid.

## When it occurs

Raised when a submitted field fails validation — wrong type, out of range, or malformed.

## How to resolve

Correct the field to match the expected type and format.
