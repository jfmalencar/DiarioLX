# Invalid username

> HTTP `400` · `application/problem+json`

**Code:** `invalid-username`

The provided username format is invalid.

## When it occurs

Raised during registration or profile updates when the username does not meet the length or allowed-character rules.

## How to resolve

Choose a username that matches the required format.
